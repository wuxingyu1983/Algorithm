// https://codeforces.com/problemset/problem/706/D

#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#include <stdio.h>
#include <string.h>
#include <iostream>
#include <fstream>
#include <set>
#include <queue>
#include <stack>
#include <map>

using namespace std;

int main()
{
    int q;
    scanf("%d", &q);

    for (size_t i = 0; i < q; i++)
    {
        char op;
        int x;

        scanf("%c %d", &op, &x);

        switch (op)
        {
        case '+':
        {
        }
        break;
        case '-':
        {
        }
        break;
        case '?':
        default:
            break;
        }
    }

    return 0;
}
