#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#include <stdio.h>
#include <string.h>
#include <iostream>
#include <fstream>
#include <iomanip>

using namespace std;

int main()
{
    unsigned int m, n;

    while (true)
    {
        cin >> n >> m;

        if (0 == n && 0 == m)
        {
            break;
        }

        unsigned int count = 0;

        

        cout << count << endl;
    }

    return 0;
}
